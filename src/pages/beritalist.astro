---
import MainLayout from "../layouts/MainLayout.astro";
import Pdf from "../components/Pdf.astro";
import { slugify } from "../utils/slugify";
import { isRenderTemplateResult } from "astro/runtime/server/render/astro/render-template.js";


---
<style>
.slider-wrapper {
  position: relative;
  overflow: hidden;
  border-radius: 16px;
  box-shadow: 0 20px 40px rgba(0,0,0,.15);
}

.slider-track {
  display: flex;
  transition: transform .6s ease;
}

.slider-item {
  min-width: 100%;
  position: relative;
}
.slider-track {
  will-change: transform;
}

.slider-item img {
  width: 100%;
  height: 380px;
  object-fit: cover;
  display: block;
}

.caption {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 20px;
  background: linear-gradient(transparent, rgba(0,0,0,.7));
  color: #fff;
}

.caption h3 {
  font-size: 18px;
  font-weight: bold;
}

.caption p {
  font-size: 14px;
  opacity: .9;
}

/* NAV */
.nav {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(255,255,255,.9);
  border-radius: 50%;
  width: 42px;
  height: 42px;
  border: none;
  cursor: pointer;
  font-size: 20px;
}

.nav:hover {
  background: #ffd41d;
}

.prev { left: 12px; }
.next { right: 12px; }


        .slider-container {
            position: relative;
            overflow: hidden;
            border-radius: 1rem;
        }

        .slider-track {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        .slider-slide {
            min-width: 100%;
            position: relative;
        }

        .slider-img {
            width: 100%;
            height: 64vh;
            max-height: 400px;
            object-fit: cover;
            border-radius: 0.5rem;
        }

        .slider-caption {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
            color: white;
            padding: 1.5rem;
            border-bottom-left-radius: 0.5rem;
            border-bottom-right-radius: 0.5rem;
        }

        .slider-nav {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 10;
            transition: all 0.3s;
        }

        .slider-nav:hover {
            background: rgba(255, 255, 255, 1);
            transform: translateY(-50%) scale(1.1);
        }

        .slider-prev {
            left: 10px;
        }

        .slider-next {
            right: 10px;
        }

        .slider-dots {
            display: flex;
            justify-content: center;
            margin-top: 1rem;
            gap: 0.5rem;
        }

        .slider-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background-color: rgba(59, 130, 246, 0.5);
            cursor: pointer;
            transition: all 0.3s;
        }

        .slider-dot.active {
            background-color: rgb(59, 130, 246);
            transform: scale(1.2);
        }

        .slider-dot:hover {
            background-color: rgb(59, 130, 246);
        }

        /* Bingkai biru dan kuning solid */
        .blue-yellow-frame {
            border: 3px solid #1d4ed8;
            /* Biru tua */
            border-top-width: 3px;
            border-left-width: 3px;
            border-right: 3px solid #fbbf24;
            /* Kuning */
            border-bottom: 3px solid #fbbf24;
            /* Kuning */
            border-bottom-right-radius: 1.5rem;
            border-top-left-radius: 1.5rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        /*pdf viewer */

        
</style>

<MainLayout title="DPMPTSP Kota Surabaya">
  <body class=" bg-gray-50 text-gray-800 antialiased"> <!-- Header -->  <!-- JavaScript untuk interaksi --> <script>
        // Update tahun di footer
        document.getElementById('currentYear').textContent = new Date().getFullYear();
        
        // Mobile menu toggle
        document.getElementById('mobile-menu-button').addEventListener('click', function() {
            const mobileMenu = document.getElementById('mobile-menu');
            mobileMenu.classList.toggle('hidden');
            
            // Tambahkan animasi slide down/up
            if (!mobileMenu.classList.contains('hidden')) {
                mobileMenu.classList.add('animate-slide-up');
            }
        });
        
        // Tab Navigation Functionality
        document.addEventListener('DOMContentLoaded', function() {
            const tabButtons = document.querySelectorAll('.tab-btn');
            const tabContents = document.querySelectorAll('.tab-content');
            
            // Show first tab by default
            if (tabContents.length > 0) {
                tabContents.forEach((content, index) => {
                    if (index === 0) {
                        content.classList.remove('hidden');
                        content.classList.add('block');
                        content.classList.add('animate-blur-in');
                    } else {
                        content.classList.add('hidden');
                        content.classList.remove('block');
                    }
                });
            }
            
            // Add click event to tab buttons
            tabButtons.forEach(button => {
                button.addEventListener('click', function() {
                    const tabId = this.getAttribute('data-tab');
                    
                    // Update active button styling
                    tabButtons.forEach(btn => {
                        btn.classList.remove('bg-blue-600', 'text-white');
                        btn.classList.add('bg-gray-100', 'hover:bg-blue-50', 'text-gray-700');
                    });
                    
                    this.classList.remove('bg-gray-100', 'hover:bg-blue-50', 'text-gray-700');
                    this.classList.add('bg-blue-600', 'text-white');
                    
                    // Show selected tab content dengan animasi
                    tabContents.forEach(content => {
                        content.classList.add('hidden');
                        content.classList.remove('block', 'animate-blur-in');
                    });
                    
                    const selectedTab = document.getElementById(tabId);
                    if (selectedTab) {
                        selectedTab.classList.remove('hidden');
                        selectedTab.classList.add('block');
                        // Delay sedikit untuk trigger animation
                        setTimeout(() => {
                            selectedTab.classList.add('animate-blur-in');
                        }, 10);
                    } else {
                        // Jika tab tidak ditemukan, tampilkan default
                        const defaultTab = document.getElementById('default-tab');
                        defaultTab.classList.remove('hidden');
                        defaultTab.classList.add('block');
                        setTimeout(() => {
                            defaultTab.classList.add('animate-blur-in');
                        }, 10);
                    }
                });
            });
            
            // FAQ Accordion Functionality
            const accordionButtons = document.querySelectorAll('.accordion-button');
            
            accordionButtons.forEach(button => {
                button.addEventListener('click', function() {
                    const targetId = this.getAttribute('data-target');
                    const targetContent = document.getElementById(targetId);
                    const icon = this.querySelector('i.fa-chevron-down');
                    
                    // Tutup semua accordion lainnya
                    accordionButtons.forEach(btn => {
                        if (btn !== this) {
                            const otherTargetId = btn.getAttribute('data-target');
                            const otherContent = document.getElementById(otherTargetId);
                            const otherIcon = btn.querySelector('i.fa-chevron-down');
                            
                            if (otherContent && !otherContent.classList.contains('hidden')) {
                                otherContent.classList.add('hidden');
                                otherIcon.style.transform = 'rotate(0deg)';
                            }
                        }
                    });
                    
                    // Toggle content visibility dengan animasi
                    if (targetContent.classList.contains('hidden')) {
                        targetContent.classList.remove('hidden');
                        targetContent.classList.add('animate-fold-unfold');
                        icon.style.transform = 'rotate(180deg)';
                        
                        // Hapus class animasi setelah selesai
                        setTimeout(() => {
                            targetContent.classList.remove('animate-fold-unfold');
                        }, 600);
                    } else {
                        targetContent.classList.add('hidden');
                        icon.style.transform = 'rotate(0deg)';
                    }
                });
            });
            
            // Scroll animations
            function animateOnScroll() {
                const elements = document.querySelectorAll('.animate-fade-up, .animate-scale-in, .animate-slide-in-left, .animate-slide-in-right');
                
                elements.forEach(element => {
                    const elementPosition = element.getBoundingClientRect().top;
                    const screenPosition = window.innerHeight / 1.2;
                    
                    if (elementPosition < screenPosition) {
                        element.classList.add('animate__animated');
                        if (element.classList.contains('animate-fade-up')) {
                            element.style.animationPlayState = 'running';
                        }
                    }
                });
            }
            
            // Run animations on scroll
            window.addEventListener('scroll', animateOnScroll);
            window.addEventListener('load', () => {
                animateOnScroll();
                // Trigger initial animations
                setTimeout(() => {
                    document.querySelectorAll('.stagger-children > *').forEach((child, index) => {
                        child.style.animationDelay = `${index * 0.1}s`;
                        child.style.animationPlayState = 'running';
                    });
                }, 300);
            });
            
            // Inisialisasi slideshow
            initSlideshow();
            
            // Tambahkan animasi pada load
            document.body.classList.add('loaded');
            
            // Mobile dropdown functionality
            const mobileDropdownBtns = document.querySelectorAll('.mobile-dropdown-btn');
            mobileDropdownBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    const content = this.nextElementSibling;
                    const icon = this.querySelector('i');
                    
                    if (content.classList.contains('hidden')) {
                        content.classList.remove('hidden');
                        icon.style.transform = 'rotate(180deg)';
                    } else {
                        content.classList.add('hidden');
                        icon.style.transform = 'rotate(0deg)';
                    }
                });
            });
        });

        // Fungsi universal untuk zoom gambar
        function openImageZoom(imageSrc, title = '') {
            const modal = document.getElementById('universal-image-modal');
            const zoomedImg = document.getElementById('universal-zoomed-image');
            const imageTitle = document.getElementById('image-title');
            
            // Set gambar dan judul
            zoomedImg.src = imageSrc;
            zoomedImg.alt = title + ' - Zoom View';
            imageTitle.textContent = title;
            
            // Tampilkan modal dengan animasi
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            document.body.style.overflow = 'hidden';
            
            // Tambahkan animasi
            setTimeout(() => {
                modal.classList.add('animate-blur-in');
            }, 10);
        }

        // Fungsi untuk menutup zoom
        function closeImageZoom() {
            const modal = document.getElementById('universal-image-modal');
            modal.classList.remove('animate-blur-in', 'flex');
            modal.classList.add('hidden');
            document.body.style.overflow = 'auto';
        }

        // Tutup modal ketika klik di luar gambar
        document.getElementById('universal-image-modal').addEventListener('click', function(e) {
            if (e.target.id === 'universal-image-modal') {
                closeImageZoom();
            }
        });

        // Tutup dengan tombol ESC
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                closeImageZoom();
            }
        });

        // VARIABEL DAN FUNGSI SLIDESHOW
        let slideIntervalId;
        const slideInterval = 7000;
        let currentSlide = 0;

        // Fungsi untuk menampilkan slide berdasarkan index
        function showSlideByIndex(index) {
            const slides = document.querySelectorAll('.main-slide');
            const dots = document.querySelectorAll('.slide-dot');
            
            if (index < 0 || index >= slides.length) return;
            
            // Animasi fade out untuk slide saat ini
            slides.forEach(slide => {
                slide.style.transition = 'opacity 0.7s cubic-bezier(0.4, 0, 0.2, 1)';
                slide.style.opacity = '0';
                slide.style.zIndex = '1';
            });
            
            // Animasi fade in untuk slide baru
            slides[index].style.transition = 'opacity 0.7s cubic-bezier(0.4, 0, 0.2, 1)';
            slides[index].style.opacity = '1';
            slides[index].style.zIndex = '2';
            
            // Update dot aktif dengan animasi
            dots.forEach(dot => {
                dot.classList.remove('bg-white');
                dot.classList.add('bg-white/50');
                dot.classList.remove('scale-125');
            });
            if (dots[index]) {
                dots[index].classList.remove('bg-white/50');
                dots[index].classList.add('bg-white');
                dots[index].classList.add('scale-125');
            }
            
            currentSlide = index;
            
            // Reset interval
            clearInterval(slideIntervalId);
            slideIntervalId = setInterval(nextSlide, slideInterval);
            
            // Tambahkan efek visual pada gambar kecil yang diklik
            const smallImages = document.querySelectorAll('[data-slide-index]');
            smallImages.forEach(img => {
                const parentDiv = img.closest('div');
                const imgIndex = parseInt(img.getAttribute('data-slide-index'));
                if (!isNaN(imgIndex) && imgIndex === index) {
                    // Tambahkan efek highlight dengan animasi
                    parentDiv.classList.add('ring-4', 'ring-yellow-400', 'ring-offset-2', 'scale-110');
                    parentDiv.classList.add('animate-jelly');
                    
                    // Kembalikan ke normal setelah 1 detik
                    setTimeout(() => {
                        parentDiv.classList.remove('ring-4', 'ring-yellow-400', 'ring-offset-2', 'scale-110', 'animate-jelly');
                    }, 1000);
                } else {
                    parentDiv.classList.remove('ring-4', 'ring-yellow-400', 'ring-offset-2', 'scale-110', 'animate-jelly');
                }
            });
        }

        // Fungsi untuk slide berikutnya
        function nextSlide() {
            const slides = document.querySelectorAll('.main-slide');
            let nextIndex = currentSlide + 1;
            if (nextIndex >= slides.length) {
                nextIndex = 0;
            }
            showSlideByIndex(nextIndex);
        }

        // Fungsi untuk inisialisasi slideshow
        function initSlideshow() {
            const slides = document.querySelectorAll('.main-slide');
            const dots = document.querySelectorAll('.slide-dot');
            
            if (slides.length === 0) return;
            
            // Tampilkan slide pertama dengan animasi
            setTimeout(() => {
                showSlideByIndex(0);
            }, 500);
            
            // Tambahkan event listener untuk dots
            dots.forEach(dot => {
                dot.addEventListener('click', function() {
                    const index = parseInt(this.getAttribute('data-index'));
                    if (!isNaN(index)) {
                        showSlideByIndex(index);
                    }
                });
            });
            
            // Mulai slideshow otomatis
            slideIntervalId = setInterval(nextSlide, slideInterval);
            
            // Pause slideshow saat hover
            const slideshowContainer = document.getElementById('main-slideshow');
            if (slideshowContainer) {
                slideshowContainer.addEventListener('mouseenter', function() {
                    clearInterval(slideIntervalId);
                });
                
                slideshowContainer.addEventListener('mouseleave', function() {
                    slideIntervalId = setInterval(nextSlide, slideInterval);
                });
            }
        }

        // Artikel Nested Dropdown (Desktop)
        const artikelWrapper = document.querySelector('.artikel-wrapper');
        const artikelContent = document.querySelector('.artikel-content');
        const artikelChevron = document.querySelector('.artikel-chevron');

        if (artikelWrapper && artikelContent) {
            artikelWrapper.addEventListener('mouseenter', () => {
                artikelContent.style.maxHeight = '200px';
                artikelContent.style.opacity = '1';
                if (artikelChevron) {
                    artikelChevron.style.transform = 'rotate(180deg)';
                }
            });

            artikelWrapper.addEventListener('mouseleave', () => {
                artikelContent.style.maxHeight = '0px';
                artikelContent.style.opacity = '0';
                if (artikelChevron) {
                    artikelChevron.style.transform = 'rotate(0deg)';
                }
            });
        }

        // Smooth scroll untuk anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                
                if (href !== '#' && href.startsWith('#')) {
                    e.preventDefault();
                    const targetId = href.substring(1);
                    const targetElement = document.getElementById(targetId);
                    
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                }
            });
        });

        // Tambahkan efek ripple pada semua tombol dengan class ripple
        document.querySelectorAll('.ripple').forEach(button => {
            button.addEventListener('click', function(e) {
                const x = e.clientX - e.target.getBoundingClientRect().left;
                const y = e.clientY - e.target.getBoundingClientRect().top;
                
                const ripple = document.createElement('span');
                ripple.style.left = `${x}px`;
                ripple.style.top = `${y}px`;
                ripple.classList.add('ripple-effect');
                
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Tambahkan style untuk ripple effect
        const style = document.createElement('style');
        style.textContent = `
            .ripple-effect {
                position: absolute;
                border-radius: 50%;
                background: rgba(255, 255, 255, 0.6);
                transform: scale(0);
                animation: ripple 0.6s linear;
                pointer-events: none;
            }
            
            @keyframes ripple {
                to {
                    transform: scale(4);
                    opacity: 0;
                }
            }
            
            .ripple {
                position: relative;
                overflow: hidden;
            }
        `;
        document.head.appendChild(style);
    </script> <!-- Content --> <main class="min-h-screen">   <section class="relative overflow-hidden bg-gradient-to-r from-blue-800 to-blue-900 py-16 text-center text-white"> <h1 class="text-4xl md:text-5xl font-bold mb-4">Berita, Artikel &amp; Video</h1> <p class="text-xl opacity-90 max-w-3xl mx-auto">Informasi terkini seputar kegiatan, perkembangan, dan artikel dari DPMPTSP Surabaya</p> <div class="mt-8 space-x-4"> <a href="/" class="inline-flex items-center bg-yellow-400 text-blue-800 font-bold px-6 py-3 rounded-lg hover:bg-yellow-300 transition"> <i class="fas fa-arrow-left mr-2"></i> Kembali ke Beranda
</a> <a href="#berita-content" class="inline-flex items-center bg-white text-blue-600 font-bold px-6 py-3 rounded-lg hover:bg-gray-100 transition"> <i class="fas fa-newspaper mr-2"></i> Lihat Berita Terbaru
</a> </div> </section>  <div class="container mx-auto px-4 py-12 grid grid-cols-1 lg:grid-cols-4 gap-8"> <!-- Video List Sidebar --> <div class="lg:col-span-1 bg-white rounded-2xl shadow-custom p-6 sticky top-24 max-h-[750px] overflow-y-auto"> <h2 class="text-xl font-bold mb-6 flex items-center"><i class="fas fa-play-circle text-blue-700 mr-2"></i>Video Terbaru</h2> <div class="group cursor-pointer card-hover bg-gray-50 p-4 rounded-xl mb-4"> <div class="flex gap-3"> <div class="relative flex-shrink-0 w-24 h-16 rounded-lg overflow-hidden"> <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRGnC6XYj-H76IMM2zB6CHlSLIIV5SVPWbdPw&amp;s" alt="Pesona Buaya September 2024" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"> <div class="absolute inset-0 bg-black/40 flex items-center justify-center"> <i class="fas fa-play text-white text-lg"></i> </div> <span class="absolute bottom-1 right-1 bg-black/80 text-white text-xs px-1 rounded"></span> </div> <div class="flex-1"> <h3 class="font-semibold text-gray-900 group-hover:text-blue-700 line-clamp-2 text-sm">Pesona Buaya September 2024</h3> <p class="text-xs text-gray-500 mt-1"><i class="far fa-eye mr-1"></i></p> <p class="text-xs text-gray-500"><i class="far fa-clock mr-1"></i> 15/9/2024</p> </div> </div> </div><div class="group cursor-pointer card-hover bg-gray-50 p-4 rounded-xl mb-4"> <div class="flex gap-3"> <div class="relative flex-shrink-0 w-24 h-16 rounded-lg overflow-hidden"> <img src="https://api.minio.jatimprov.go.id/kominfo-jatim/images/IMG_8752.JPG" alt="Surabaya Great Expo 2024" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"> <div class="absolute inset-0 bg-black/40 flex items-center justify-center"> <i class="fas fa-play text-white text-lg"></i> </div> <span class="absolute bottom-1 right-1 bg-black/80 text-white text-xs px-1 rounded"></span> </div> <div class="flex-1"> <h3 class="font-semibold text-gray-900 group-hover:text-blue-700 line-clamp-2 text-sm">Surabaya Great Expo 2024</h3> <p class="text-xs text-gray-500 mt-1"><i class="far fa-eye mr-1"></i></p> <p class="text-xs text-gray-500"><i class="far fa-clock mr-1"></i> 20/8/2024</p> </div> </div> </div><div class="group cursor-pointer card-hover bg-gray-50 p-4 rounded-xl mb-4"> <div class="flex gap-3"> <div class="relative flex-shrink-0 w-24 h-16 rounded-lg overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/tentangfile/PESONA%20BUAYA%20-2025-07-15()_0.png" alt="Pesona Buaya Juli 2024" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"> <div class="absolute inset-0 bg-black/40 flex items-center justify-center"> <i class="fas fa-play text-white text-lg"></i> </div> <span class="absolute bottom-1 right-1 bg-black/80 text-white text-xs px-1 rounded"></span> </div> <div class="flex-1"> <h3 class="font-semibold text-gray-900 group-hover:text-blue-700 line-clamp-2 text-sm">Pesona Buaya Juli 2024</h3> <p class="text-xs text-gray-500 mt-1"><i class="far fa-eye mr-1"></i></p> <p class="text-xs text-gray-500"><i class="far fa-clock mr-1"></i> 10/7/2024</p> </div> </div> </div> <a href="#" class="block mt-6 text-center bg-blue-700 hover:bg-blue-800 text-white font-semibold py-3 px-4 rounded-lg transition card-hover"> <i class="fas fa-play-circle mr-2"></i>Lihat Semua Video
</a> </div> <!-- Berita Grid --> <div id="berita-content" class="lg:col-span-3 grid grid-cols-1 md:grid-cols-2 gap-6"> <div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/lkpm-tw4-2025-2026-01-05(BERITA).jpeg" alt="Pelaporan LKPM TW IV dan Semester II Tahun 2025 Dibuka, Pelaku Usaha Wajib Lapor 1–10 Januari 2026" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">Pelaporan LKPM TW IV dan Semester II Tahun 2025 Dibuka, Pelaku Usaha Wajib Lapor 1–10 Januari 2026</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>5/1/2026 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/apresiasi-pegawai-terbaik-2025-2026-01-05(BERITA).jpeg" alt="DPMPTSP Kota Surabaya Berikan Apresiasi Pegawai Terbaik Tahun 2025" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Surabaya Berikan Apresiasi Pegawai Terbaik Tahun 2025</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>5/1/2026 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/umkm-bkpm-2025-2025-12-16(BERITA).jpeg" alt="DPMPTSP Kota Surabaya Terima Kunjungan Kementerian Investasi dan Hilirisasi/BKPM RI untuk Fasilitasi Pelaku UMKM" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Surabaya Terima Kunjungan Kementerian Investasi dan Hilirisasi/BKPM RI untuk Fasilitasi Pelaku UMKM</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>16/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/Tanggerang-studitour-2025-2025-12-16(BERITA).jpeg" alt="Pemkot Tangerang Lakukan Studi Tiru Percepatan Pelayanan Perizinan dan Non Perizinan ke Pemkot Surabaya" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">Pemkot Tangerang Lakukan Studi Tiru Percepatan Pelayanan Perizinan dan Non Perizinan ke Pemkot Surabaya</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>16/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/tpid-2025-2025-12-16(BERITA).jpeg" alt="Pemerintah Kota Surabaya Adakan Kegiatan High Level Marketing untuk Perkuat Sinergi Kebijakan dan Pertumbuhan Ekonomi" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">Pemerintah Kota Surabaya Adakan Kegiatan High Level Marketing untuk Perkuat Sinergi Kebijakan dan Pertumbuhan Ekonomi</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>16/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/Dwp-gathering-dpmptsp-2025-2025-12-15(BERITA).jpeg" alt="DWP DPMPTSP Kota Surabaya Gelar Gathering HUT ke-26 Bertajuk “Kembali Ke Alam Kebun Raya Mangrove Bersama Yamaha”" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DWP DPMPTSP Kota Surabaya Gelar Gathering HUT ke-26 Bertajuk “Kembali Ke Alam Kebun Raya Mangrove Bersama Yamaha”</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>15/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/Madiun-kunjungan-2025-2025-12-15(BERITA).jpeg" alt="DPMPTSP Kota Surabaya Terima Studi Tiru Pelayanan Publik DPMPTSP Kabupaten Madiun" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Surabaya Terima Studi Tiru Pelayanan Publik DPMPTSP Kabupaten Madiun</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>15/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/short-course-inovasi-daerah-2025-2025-12-15(BERITA).jpeg" alt="DPMPTSP Kota Surabaya Jadi Narasumber Short Course Inovasi Daerah" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Surabaya Jadi Narasumber Short Course Inovasi Daerah</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>15/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/Yogyakarta-2025-2025-12-12(BERITA).jpg" alt="DPMPTSP Kota Yogyakarta Studi Tiru ke DPMPTSP Kota Surabaya" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Yogyakarta Studi Tiru ke DPMPTSP Kota Surabaya</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>12/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div><div class="bg-white rounded-2xl shadow-custom overflow-hidden card-hover animate-fadeInUp"> <a href="/media/" class="block h-full"> <div class="flex flex-col h-full"> <div class="relative h-64 overflow-hidden"> <img src="https://dpm-ptsp.surabaya.go.id/fileberita/economic-outlook-2025-2025-12-08(BERITA).jpeg" alt="DPMPTSP Kota Surabaya Hadiri Economic Outlook 2026 yang Digelar Bisnis Indonesia Perwakilan Jawa Timur" class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"> </div> <div class="p-6 flex-grow"> <h3 class="text-lg font-bold text-blue-800 mb-3 line-clamp-2"></h3> <p class="text-gray-600 text-sm mb-4 line-clamp-3">DPMPTSP Kota Surabaya Hadiri Economic Outlook 2026 yang Digelar Bisnis Indonesia Perwakilan Jawa Timur</p> <div class="flex items-center justify-between pt-4 border-t border-gray-100 mt-auto"> <div class="text-sm text-gray-500"> <i class="far fa-calendar-alt mr-1"></i>8/12/2025 </div> <span class="text-blue-600 font-semibold text-sm hover:text-blue-800 flex items-center">
Baca Selengkapnya <i class="fas fa-arrow-right ml-1 text-xs"></i> </span> </div> </div> </div> </a> </div> </div> </div>  </main> <!-- Footer -->  <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                        },
                        secondary: {
                            400: '#fbbf24',
                            500: '#f59e0b',
                        }
                    },
                    animation: {
                        'pulse-slow': 'pulse 3s infinite',
                        'bounce-slow': 'bounce 2s infinite',
                        'spin-slow': 'spin 4s linear infinite',
                    }
                }
            }
        }
    </script>  <astro-dev-toolbar></astro-dev-toolbar></body>
  </MainLayout>
