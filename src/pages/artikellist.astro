---
export const prerender = false; // WAJIB UNTUK HTTP API

import MainLayout from "../layouts/MainLayout.astro";
import { slugify } from "../utils/slugify";

const API_URL = "http://parkir.dpmptsp-surabaya.my.id/api/v1/posts?per_page=20&page=2";

let articles = [];

try {
  const res = await fetch(API_URL);
  const json = await res.json();
  articles = json.data ?? [];
} catch (e) {
  console.error("API ERROR", e);
}
---
